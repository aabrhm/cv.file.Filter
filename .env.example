# Supabase (persistent candidate storage — required)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_BUCKET=cvs

# Gemini (CV parsing — required)
GEMINI_API_KEY=

# Dashboard login (required)
APP_LOGIN_EMAIL=
APP_LOGIN_PASSWORD=
APP_SESSION_SECRET=

# Optional
APP_ALLOWED_ORIGINS=
APP_MAX_UPLOAD_MB=10
